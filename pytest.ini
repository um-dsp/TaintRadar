[pytest]
testpaths = utils/tests

# -ra prints the reason for every skipped test, including under -q. The suite skips
# whenever Joern or a SARD corpus is missing, and a bare "80 skipped" with no reason is
# indistinguishable from a broken checkout.
addopts = -ra
